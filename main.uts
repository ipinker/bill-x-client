import App from './App.uvue'

import { createSSRApp } from 'vue'


import ultraUI from '@/x-ui/index.uts' // 导入 uview-ultra 跨端 UI 组件库
export function createApp() {
	const app = createSSRApp(App)

	return {
		app
	}
}
