# 清新记账 - 主题设计规范

**版本**: v2.1  
**设计风格**: 简洁、优雅、细致、统一  
**适用平台**: uni-app x (iOS / Android)

---

## 一、设计理念

### 1.1 核心原则

**简洁性** - 去除冗余元素，突出核心内容  
**优雅性** - 细腻的视觉处理，舒适的配色方案  
**一致性** - 统一的设计语言，流畅的交互体验  
**高效性** - 清晰的信息层次，快速的操作路径

### 1.2 设计关键词

- 清新 Fresh
- 简约 Minimal
- 优雅 Elegant
- 现代 Modern
- 轻量 Lightweight
- 直观 Intuitive

---

## 二、色彩系统

### 2.1 主色调 (Primary Color)

**紫罗兰系列（项目当前主题）**

```
主题色 Primary:        #7C4DFF
主题色浅 Light:         #F1ECFF (10% 透明度背景)
主题色中 Medium:        #9575CD (hover/active 状态)
主题色深 Dark:          #5C35CC (按下状态)
主题色阴影 Shadow:      rgba(124, 77, 255, 0.18)
```

**使用场景**
- 主要操作按钮
- Tab 选中状态
- 重要信息标识
- 链接文字
- 进度条
- 选中状态背景

### 2.2 辅助色 (Secondary Colors)

**成功色 Success - 翠绿**
```
成功色:               #39B54A
成功色浅:             #E8F8EB
成功色深:             #2E9138
```
使用场景: 收入金额、成功提示、正向反馈

**警告色 Warning - 橙黄**
```
警告色:               #F7C242
警告色浅:             #FFF9E6
警告色深:             #E0A91F
```
使用场景: 预算预警、重要提醒、次要提示

**错误色 Error - 珊瑚红**
```
错误色:               #FC5872
错误色浅:             #FFEBEE
错误色深:             #E03E56
```
使用场景: 支出金额、错误提示、删除操作、危险警告

**信息色 Info - 淡紫**
```
信息色:               #AE9AF0
信息色浅:             #F3F0FF
信息色深:             #8B6FE0
```
使用场景: 资产模块、信息提示、特殊标识

### 2.3 中性色 (Neutral Colors)

**文本颜色**
```
主文本 Text Primary:      #333333 (标题、重要内容)
次文本 Text Secondary:    #666666 (正文、描述)
辅助文本 Text Tertiary:   #999999 (说明文字、placeholder)
禁用文本 Text Disabled:   #CCCCCC (不可用状态)
```

**背景颜色**
```
页面背景 Background:      #F7F8FA (浅灰底色)
容器背景 Container:       #FFFFFF (卡片、弹窗)
浅灰背景 Light Gray:      #F5F5F5 (输入框、分割区域)
深灰背景 Dark Gray:       #E5E5E5 (禁用背景)
```

**边框颜色**
```
边框主色 Border:          #E5E5E5 (常规边框)
边框深色 Border Dark:     #CCCCCC (强调边框)
分割线 Divider:           #F0F0F0 (列表分割)
```

**特殊背景**
```
遮罩层 Mask:              rgba(0, 0, 0, 0.5)
透明背景 Transparent:     transparent
半透明白 Semi White:      rgba(255, 255, 255, 0.9)
```

### 2.4 数据可视化色板

**收支配色**
```
支出色:                   #FC5872 (珊瑚红)
收入色:                   #39B54A (翠绿)
结余正:                   #39B54A (正数用绿色)
结余负:                   #FC5872 (负数用红色)
```

**图表配色 (8色)**
```
Color 1:                  #7C4DFF (紫罗兰)
Color 2:                  #39B54A (翠绿)
Color 3:                  #F7C242 (橙黄)
Color 4:                  #FC5872 (珊瑚红)
Color 5:                  #AE9AF0 (淡紫)
Color 6:                  #00AA90 (青绿)
Color 7:                  #F0AA90 (浅橙)
Color 8:                  #B481BB (紫红)
```

### 2.5 渐变色方案

**主渐变 (导航栏、头部卡片)**
```css
linear-gradient(135deg, #7C4DFF 0%, #9575CD 100%);
```

**首页 66% 渐变氛围**
```css
linear-gradient(180deg, rgba(124,77,255,.92) 0%, rgba(149,117,205,.55) 42%, rgba(247,248,250,0) 66%, #F7F8FA 100%);
```

**登录/启动页全屏渐变**
```css
radial-gradient(circle at 18% 14%, rgba(211,194,255,.92), transparent 34%), radial-gradient(circle at 84% 32%, rgba(124,77,255,.86), transparent 42%), linear-gradient(145deg, #5C35CC 0%, #7C4DFF 48%, #B9A4F5 100%);
```

**成功渐变 (收入背景)**
```css
linear-gradient(109.6deg, #E8F8EB 29.9%, #FFFFFF 99.9%);
```

**错误渐变 (支出背景)**
```css
linear-gradient(109.6deg, #FFEBEE 29.9%, #FFFFFF 99.9%);
```

**警告渐变**
```css
linear-gradient(109.6deg, #FFF9E6 29.9%, #FFFFFF 99.9%);
```

**信息渐变**
```css
linear-gradient(109.6deg, #F3F0FF 29.9%, #FFFFFF 99.9%);
```

**页面背景渐变 (可选)**
```css
linear-gradient(180deg, #F7F8FA 0%, #FFFFFF 100%);
```

### 2.6 透明度规范

```
不透明:                   1.0 / 100%
强调层:                   0.9 / 90%
常规层:                   0.8 / 80%
次要层:                   0.6 / 60%
辅助层:                   0.4 / 40%
提示层:                   0.2 / 20%
背景层:                   0.1 / 10%
微弱层:                   0.05 / 5%
```

---

## 三、字体系统

### 3.1 字体家族

**iOS 系统字体**
```css
font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Helvetica Neue', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif;
```

**Android 系统字体**
```css
font-family: 'Roboto', 'Noto Sans SC', 'Droid Sans', sans-serif;
```

**跨平台通用**
```css
font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
```

### 3.2 字号规范 (rpx)

| 级别 | 大小 | 行高 | 使用场景 |
|------|------|------|----------|
| h1 | 48rpx | 68rpx | 页面主标题、金额超大号 |
| h2 | 40rpx | 56rpx | 重要数字、卡片大标题 |
| h3 | 36rpx | 50rpx | 金额大号、次级标题 |
| h4 | 32rpx | 46rpx | 导航栏标题、卡片标题 |
| h5 | 30rpx | 42rpx | 小标题 |
| body-large | 28rpx | 40rpx | 列表项主文字、重要内容 |
| body | 26rpx | 38rpx | 正文、表单标签 |
| body-small | 24rpx | 36rpx | 辅助说明、次要信息 |
| caption | 22rpx | 32rpx | 小字提示、时间戳 |
| mini | 20rpx | 28rpx | 极小文字、角标 |

### 3.3 字重规范

| 字重 | 数值 | 使用场景 |
|------|------|----------|
| Light | 300 | 大面积辅助文字 |
| Regular | 400 | 正文、默认文本 |
| Medium | 500 | 强调文本、按钮文字 |
| Semibold | 600 | 小标题、列表标题 |
| Bold | 700 | 大标题、重要数字 |

### 3.4 文本样式组合

**超大金额**
```css
font-size: 48rpx;
font-weight: 700;
line-height: 68rpx;
color: #7C4DFF;
text-shadow: 0 0 4rpx rgba(124, 77, 255, 0.3);
```

**标题文字**
```css
font-size: 32rpx;
font-weight: 600;
line-height: 46rpx;
color: #333333;
```

**正文内容**
```css
font-size: 26rpx;
font-weight: 400;
line-height: 38rpx;
color: #666666;
```

**辅助说明**
```css
font-size: 24rpx;
font-weight: 400;
line-height: 36rpx;
color: #999999;
```

---

## 四、间距系统

### 4.1 基础间距单位

基于 **8rpx** 栅格系统

| 变量名 | 数值 | 使用场景 |
|--------|------|----------|
| space-xs | 8rpx | 极小间距 |
| space-sm | 16rpx | 小间距 |
| space-md | 24rpx | 中间距 |
| space-lg | 32rpx | 大间距 |
| space-xl | 40rpx | 超大间距 |
| space-xxl | 48rpx | 模块间距 |

### 4.2 页面边距

```css
/* 页面左右边距 */
padding: 0 30rpx;

/* 页面顶部间距 */
margin-top: 40rpx;

/* 页面底部安全距离 */
padding-bottom: calc(120rpx + env(safe-area-inset-bottom));
```

### 4.3 组件内边距

**卡片内边距**
```css
padding: 30rpx;
```

**列表项内边距**
```css
padding: 30rpx 40rpx;
```

**按钮内边距**
```css
/* 大按钮 */
padding: 0 60rpx;

/* 中按钮 */
padding: 0 40rpx;

/* 小按钮 */
padding: 0 30rpx;
```

**输入框内边距**
```css
padding: 0 30rpx;
```

### 4.4 组件间距

**卡片间距**
```css
margin-bottom: 30rpx;
```

**列表项间距**
```css
margin-bottom: 0; /* 无间距，通过分割线区分 */
```

**表单项间距**
```css
margin-bottom: 40rpx;
```

**段落间距**
```css
margin-bottom: 24rpx;
```

---

## 五、圆角系统

### 5.1 圆角规范

| 级别 | 数值 | 使用场景 |
|------|------|----------|
| radius-xs | 8rpx | 小标签、角标 |
| radius-sm | 12rpx | 输入框、小卡片 |
| radius-md | 16rpx | 按钮、标签 |
| radius-lg | 24rpx | 卡片、弹窗 |
| radius-xl | 32rpx | 大卡片 |
| radius-round | 50% | 圆形头像、图标按钮 |
| radius-pill | 999rpx | 胶囊按钮 |

### 5.2 应用示例

**卡片圆角**
```css
border-radius: 24rpx;
```

**按钮圆角**
```css
/* 常规按钮 */
border-radius: 16rpx;

/* 圆形按钮 */
border-radius: 50%;

/* 胶囊按钮 */
border-radius: 44rpx; /* 高度的一半 */
```

**输入框圆角**
```css
border-radius: 12rpx;
```

**头像圆角**
```css
border-radius: 50%; /* 圆形 */
/* 或 */
border-radius: 16rpx; /* 圆角矩形 */
```

---

## 六、阴影系统

### 6.1 阴影级别

**Level 0 - 无阴影**
```css
box-shadow: none;
```

**Level 1 - 轻微阴影 (悬浮卡片)**
```css
box-shadow: 0 2rpx 8rpx 0 rgba(0, 0, 0, 0.04);
```

**Level 2 - 常规阴影 (卡片)**
```css
box-shadow: 0 4rpx 20rpx 0 rgba(0, 0, 0, 0.08);
```

**Level 3 - 中等阴影 (弹窗)**
```css
box-shadow: 0 8rpx 30rpx 0 rgba(0, 0, 0, 0.12);
```

**Level 4 - 强阴影 (Modal)**
```css
box-shadow: 0 12rpx 40rpx 0 rgba(0, 0, 0, 0.16);
```

**Level 5 - 超强阴影 (Drawer)**
```css
box-shadow: 0 16rpx 60rpx 0 rgba(0, 0, 0, 0.20);
```

### 6.2 特殊阴影

**导航栏底部阴影**
```css
box-shadow: 0 2rpx 10rpx 0 rgba(0, 0, 0, 0.05);
```

**按钮点击阴影**
```css
box-shadow: 0 4rpx 12rpx 0 rgba(124, 77, 255, 0.3);
```

**文字阴影 (发光效果)**
```css
text-shadow: 0 0 4rpx rgba(124, 77, 255, 0.3);
```

**内阴影 (凹陷效果)**
```css
box-shadow: inset 0 2rpx 4rpx 0 rgba(0, 0, 0, 0.06);
```

---

## 七、图标系统

### 7.1 图标尺寸

| 级别 | 大小 | 使用场景 |
|------|------|----------|
| mini | 24rpx | 按钮内小图标 |
| small | 32rpx | 列表项图标 |
| medium | 40rpx | Tab 图标、常规图标 |
| large | 48rpx | 功能入口大图标 |
| xlarge | 64rpx | 空状态图标 |
| xxlarge | 96rpx | 启动页图标 |

### 7.2 图标风格

**线性图标 (主要)**
- 描边粗细: 1.8-2px
- 端点样式: 圆角
- 连接样式: 圆角

**面性图标 (辅助)**
- 仅用于 AI 中间按钮、启动页和特殊空状态
- 普通功能入口不使用 emoji 或混杂插画图标

**双色图标 (强调)**
- 主色: 主题色
- 辅色: 中性色

### 7.3 图标颜色

**默认状态**
```css
color: #666666;
```

**激活状态**
```css
color: #7C4DFF;
```

**禁用状态**
```css
color: #CCCCCC;
```

**错误状态**
```css
color: #FC5872;
```

**成功状态**
```css
color: #39B54A;
```

### 7.4 图标库

使用 iconfont 字体图标库

**引入方式**
```css
@font-face {
  font-family: 'iconfont';
  src: url('/static/css/font/iconfont.ttf');
}

.iconfont {
  font-family: 'iconfont' !important;
  font-style: normal;
}
```

---

## 八、按钮规范

### 8.1 按钮类型

#### 主按钮 (Primary)

**样式**
```css
background: #7C4DFF;
color: #FFFFFF;
height: 88rpx;
border-radius: 16rpx;
font-size: 28rpx;
font-weight: 500;
```

**激活态**
```css
background: #5C35CC;
```

**禁用态**
```css
background: #E5E5E5;
color: #CCCCCC;
```

#### 次要按钮 (Secondary)

**样式**
```css
background: #FFFFFF;
color: #7C4DFF;
border: 2rpx solid #7C4DFF;
height: 88rpx;
border-radius: 16rpx;
font-size: 28rpx;
font-weight: 500;
```

#### 文字按钮 (Text)

**样式**
```css
background: transparent;
color: #7C4DFF;
height: auto;
padding: 10rpx 20rpx;
font-size: 26rpx;
```

#### 图标按钮 (Icon)

**圆形**
```css
width: 60rpx;
height: 60rpx;
border-radius: 50%;
background: rgba(124, 77, 255, 0.1);
color: #7C4DFF;
font-size: 32rpx;
```

**方形**
```css
width: 60rpx;
height: 60rpx;
border-radius: 12rpx;
background: rgba(124, 77, 255, 0.1);
color: #7C4DFF;
font-size: 32rpx;
```

### 8.2 按钮尺寸

| 尺寸 | 高度 | 内边距 | 字号 | 使用场景 |
|------|------|--------|------|----------|
| large | 96rpx | 0 80rpx | 32rpx | 主要操作 |
| default | 88rpx | 0 60rpx | 28rpx | 常规按钮 |
| medium | 72rpx | 0 48rpx | 26rpx | 表单按钮 |
| small | 60rpx | 0 36rpx | 24rpx | 次要操作 |
| mini | 48rpx | 0 24rpx | 22rpx | 卡片内按钮 |

### 8.3 按钮状态

**正常态 (Normal)**
- 默认样式

**悬停态 (Hover) - Web Only**
```css
opacity: 0.9;
```

**激活态 (Active)**
```css
opacity: 0.8;
transform: scale(0.98);
```

**禁用态 (Disabled)**
```css
background: #E5E5E5;
color: #CCCCCC;
cursor: not-allowed;
```

**加载态 (Loading)**
- 显示 loading 图标
- 禁用点击

---

## 九、输入框规范

### 9.1 基础样式

**默认状态**
```css
height: 88rpx;
border: 2rpx solid #E5E5E5;
border-radius: 12rpx;
padding: 0 30rpx;
background: #FFFFFF;
font-size: 26rpx;
color: #333333;
```

**占位符**
```css
placeholder {
  color: #CCCCCC;
  font-size: 26rpx;
}
```

**聚焦状态**
```css
border-color: #7C4DFF;
background: #FFFFFF;
```

**禁用状态**
```css
background: #F5F5F5;
color: #CCCCCC;
border-color: #E5E5E5;
```

**错误状态**
```css
border-color: #FC5872;
background: #FFEBEE;
```

### 9.2 输入框类型

**单行文本框**
```css
height: 88rpx;
```

**多行文本框 (Textarea)**
```css
min-height: 120rpx;
padding: 20rpx 30rpx;
line-height: 40rpx;
```

**搜索框**
```css
border-radius: 44rpx; /* 胶囊形 */
padding-left: 60rpx; /* 为搜索图标留空间 */
```

**数字输入框**
```css
text-align: right; /* 金额输入右对齐 */
font-size: 36rpx; /* 金额输入大号字体 */
font-weight: 600;
```

---

## 十、卡片规范

### 10.1 基础卡片

**样式**
```css
background: #FFFFFF;
border-radius: 24rpx;
padding: 30rpx;
box-shadow: 0 4rpx 20rpx 0 rgba(0, 0, 0, 0.08);
margin-bottom: 30rpx;
```

### 10.2 卡片变体

**简洁卡片 (无阴影)**
```css
background: #FFFFFF;
border-radius: 24rpx;
padding: 30rpx;
border: 2rpx solid #F0F0F0;
```

**透明卡片**
```css
background: rgba(255, 255, 255, 0.9);
border-radius: 24rpx;
padding: 30rpx;
backdrop-filter: blur(20rpx);
```

**带头部卡片**
```html
<view class="card">
  <view class="card-header">标题</view>
  <view class="card-body">内容</view>
</view>
```

```css
.card-header {
  padding-bottom: 20rpx;
  border-bottom: 2rpx solid #F0F0F0;
  font-size: 28rpx;
  font-weight: 600;
  color: #333333;
}

.card-body {
  padding-top: 20rpx;
}
```

---

## 十一、列表规范

### 11.1 列表项

**基础列表项**
```css
padding: 30rpx 40rpx;
background: #FFFFFF;
border-bottom: 2rpx solid #F0F0F0;
```

**最后一项**
```css
border-bottom: none;
```

**激活态**
```css
background: #F7F8FA;
```

### 11.2 列表项布局

**左图标 + 文字 + 右箭头**
```html
<view class="list-item">
  <view class="list-item-icon"></view>
  <view class="list-item-content">
    <view class="list-item-title">标题</view>
    <view class="list-item-desc">描述</view>
  </view>
  <view class="list-item-arrow"></view>
</view>
```

**账单列表项**
```html
<view class="bill-item">
  <view class="bill-icon"></view>
  <view class="bill-content">
    <view class="bill-title">标题</view>
    <view class="bill-remark">备注</view>
  </view>
  <view class="bill-amount">金额</view>
</view>
```

---

## 十二、导航栏规范

### 12.1 标准导航栏

**高度**
```css
/* iOS */
height: 44px + statusBarHeight;

/* Android */
height: 48px + statusBarHeight;

/* uni-app x 统一 */
height: calc(88rpx + var(--status-bar-height));
```

**样式**
```css
background: #FFFFFF;
box-shadow: 0 2rpx 10rpx 0 rgba(0, 0, 0, 0.05);
```

**标题**
```css
font-size: 32rpx;
font-weight: 600;
color: #333333;
```

**按钮**
```css
width: 60rpx;
height: 60rpx;
font-size: 32rpx;
color: #333333;
```

### 12.2 自定义导航栏

**渐变背景**
```css
background: linear-gradient(135deg, #7C4DFF 0%, #9575CD 100%);
color: #FFFFFF;
```

**透明导航栏**
```css
background: transparent;
position: absolute;
top: 0;
left: 0;
right: 0;
z-index: 100;
```

---

## 十三、TabBar 规范

### 13.1 底部 TabBar

**高度**
```css
height: calc(100rpx + env(safe-area-inset-bottom));
```

**样式**
```css
background: rgba(255, 255, 255, 0.94);
border-radius: 44rpx;
box-shadow: 0 8rpx 30rpx 0 rgba(39, 28, 85, 0.14);
```

**Tab 项**
```css
.tab-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 10rpx 0;
}

.tab-icon {
  font-size: 40rpx;
  color: #999999;
}

.tab-text {
  font-size: 20rpx;
  color: #999999;
  margin-top: 6rpx;
}

.tab-item-active .tab-icon,
.tab-item-active .tab-text {
  color: #7C4DFF;
}
```

**中间 AI 凸起按钮**
```css
width: 132rpx;
height: 132rpx;
border-radius: 50%;
border: 12rpx solid #FFFFFF;
background: linear-gradient(135deg, #7C4DFF 0%, #9575CD 100%);
box-shadow: 0 18rpx 40rpx rgba(124, 77, 255, 0.36);
```

底部只放：首页、AI、我的；记一笔使用首页内容中的主操作卡承载。

### 13.2 顶部 Tab

**样式**
```css
height: 88rpx;
background: rgba(124, 77, 255, 0.1);
border-radius: 44rpx;
padding: 4rpx;
```

**Tab 项**
```css
.tab-item {
  flex: 1;
  height: 80rpx;
  line-height: 80rpx;
  text-align: center;
  font-size: 26rpx;
  color: #666666;
  border-radius: 40rpx;
  transition: all 0.3s;
}

.tab-item-active {
  background: #FFFFFF;
  color: #7C4DFF;
  font-weight: 500;
}
```

---

## 十四、弹窗规范

### 14.1 Modal 弹窗

**遮罩层**
```css
background: rgba(0, 0, 0, 0.5);
```

**弹窗容器**
```css
width: 600rpx;
background: #FFFFFF;
border-radius: 24rpx;
padding: 40rpx;
box-shadow: 0 12rpx 40rpx 0 rgba(0, 0, 0, 0.16);
```

**标题**
```css
font-size: 32rpx;
font-weight: 600;
color: #333333;
text-align: center;
margin-bottom: 20rpx;
```

**内容**
```css
font-size: 26rpx;
color: #666666;
line-height: 40rpx;
text-align: center;
margin-bottom: 40rpx;
```

**按钮组**
```css
display: flex;
gap: 20rpx;
```

### 14.2 Bottom Sheet

**样式**
```css
background: #FFFFFF;
border-radius: 24rpx 24rpx 0 0;
padding: 40rpx 30rpx;
max-height: 80vh;
```

**分享底部浮层**
- 统一提供：微信、微信朋友圈、QQ、复制链接。
- 四项等宽排列，配套统一线性图标；复制链接成功后显示 Toast。
- 使用白色圆角顶部面板、半透明遮罩和底部安全区。

**拖拽指示器**
```css
width: 60rpx;
height: 8rpx;
background: #E5E5E5;
border-radius: 4rpx;
margin: 0 auto 30rpx;
```

### 14.3 Toast 提示

**样式**
```css
background: rgba(0, 0, 0, 0.75);
color: #FFFFFF;
padding: 20rpx 40rpx;
border-radius: 16rpx;
font-size: 26rpx;
max-width: 500rpx;
text-align: center;
```

---

## 十五、动画规范

### 15.1 过渡时长

| 类型 | 时长 | 使用场景 |
|------|------|----------|
| fast | 150ms | 微小元素、悬停效果 |
| normal | 300ms | 常规过渡、颜色变化 |
| slow | 500ms | 大元素、复杂动画 |

### 15.2 缓动函数

**标准缓动**
```css
transition-timing-function: ease;
```

**进入缓动**
```css
transition-timing-function: ease-out;
```

**退出缓动**
```css
transition-timing-function: ease-in;
```

**双向缓动**
```css
transition-timing-function: ease-in-out;
```

**自定义贝塞尔**
```css
/* 轻快 */
transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);

/* 回弹 */
transition-timing-function: cubic-bezier(0.175, 0.885, 0.32, 1.275);
```

### 15.3 常用动画

**淡入淡出**
```css
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes fadeOut {
  from { opacity: 1; }
  to { opacity: 0; }
}
```

**滑入滑出**
```css
@keyframes slideInUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

@keyframes slideOutDown {
  from { transform: translateY(0); }
  to { transform: translateY(100%); }
}
```

**缩放**
```css
@keyframes scaleIn {
  from { transform: scale(0.8); opacity: 0; }
  to { transform: scale(1); opacity: 1; }
}
```

**旋转加载**
```css
@keyframes rotate {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}
```

---

## 十六、响应式设计

### 16.1 屏幕尺寸

**设计稿基准**
- 宽度: 750rpx
- 1rpx = 0.5px (iPhone 6/7/8)

**适配范围**
- 小屏: 320px - 375px (iPhone SE/5)
- 中屏: 375px - 414px (iPhone 6/7/8/X)
- 大屏: 414px+ (iPhone Plus/Pro Max)
- 平板: 768px+ (iPad)

### 16.2 安全区适配

**顶部状态栏**
```css
padding-top: var(--status-bar-height);
```

**底部安全区 (iPhone X+)**
```css
padding-bottom: calc(20rpx + env(safe-area-inset-bottom));
```

**侧边安全区**
```css
padding-left: env(safe-area-inset-left);
padding-right: env(safe-area-inset-right);
```

---

## 十七、暗黑模式 (可选)

### 17.1 色彩映射

| 浅色模式 | 暗黑模式 | 说明 |
|----------|----------|------|
| #FFFFFF | #1F1F1F | 容器背景 |
| #F7F8FA | #121212 | 页面背景 |
| #333333 | #E5E5E5 | 主文本 |
| #666666 | #CCCCCC | 次文本 |
| #999999 | #999999 | 辅助文本 |
| #E5E5E5 | #3A3A3A | 边框 |
| #F0F0F0 | #2A2A2A | 分割线 |

**主题色保持不变**
- #7C4DFF 等辅助色在暗黑模式下不变

### 17.2 实现方式

```css
/* 定义CSS变量 */
:root {
  --bg-color: #F7F8FA;
  --container-bg: #FFFFFF;
  --text-primary: #333333;
}

/* 暗黑模式 */
@media (prefers-color-scheme: dark) {
  :root {
    --bg-color: #121212;
    --container-bg: #1F1F1F;
    --text-primary: #E5E5E5;
  }
}
```

---

## 十八、无障碍设计

### 18.1 对比度要求

**正常文字 (< 18pt)**
- 最小对比度: 4.5:1 (AA)
- 增强对比度: 7:1 (AAA)

**大文字 (≥ 18pt 或 14pt bold)**
- 最小对比度: 3:1 (AA)
- 增强对比度: 4.5:1 (AAA)

### 18.2 可点击区域

**最小触摸尺寸**
- 宽度: 88rpx (44pt)
- 高度: 88rpx (44pt)

**推荐触摸尺寸**
- 宽度: 96rpx (48pt)
- 高度: 96rpx (48pt)

### 18.3 焦点指示

**聚焦样式**
```css
:focus {
  outline: 4rpx solid #7C4DFF;
  outline-offset: 4rpx;
}
```

---

## 十九、设计资源

### 19.1 Figma 组件库

包含以下组件：
- ✅ 按钮 (6 种类型 × 5 种尺寸)
- ✅ 输入框 (4 种状态)
- ✅ 卡片 (3 种变体)
- ✅ 列表项 (5 种布局)
- ✅ 导航栏 (3 种样式)
- ✅ TabBar (底部 + 顶部)
- ✅ 弹窗 (Modal + Sheet + Toast)
- ✅ 图标库 (100+ 图标)
- ✅ 色板 + 字体样式

### 19.2 设计文件

- **Figma 链接**: [待补充]
- **Sketch 文件**: [待补充]
- **Adobe XD 文件**: [待补充]

### 19.3 切图规范

**导出倍率**
- 1x (mdpi)
- 2x (xhdpi) - 主要
- 3x (xxhdpi)

**图片格式**
- PNG: 图标、带透明度的图片
- JPG: 照片、背景图
- SVG: 矢量图标(优先)
- WebP: 优化后的图片

**命名规范**
```
ic_[类型]_[名称]_[状态]@[倍率].png

示例:
ic_nav_back_normal@2x.png
ic_tab_home_selected@2x.png
bg_gradient_header@2x.png
```

---

## 二十、开发实施

### 20.1 CSS 变量定义

```css
/* colors.css */
:root {
  /* 主色调 */
  --color-primary: #7C4DFF;
  --color-primary-light: #E6F3FF;
  --color-primary-dark: #5C35CC;
  
  /* 辅助色 */
  --color-success: #39B54A;
  --color-warning: #F7C242;
  --color-error: #FC5872;
  --color-info: #AE9AF0;
  
  /* 中性色 */
  --text-primary: #333333;
  --text-secondary: #666666;
  --text-tertiary: #999999;
  --text-disabled: #CCCCCC;
  
  --bg-page: #F7F8FA;
  --bg-container: #FFFFFF;
  --border-color: #E5E5E5;
  --divider-color: #F0F0F0;
  
  /* 间距 */
  --space-xs: 8rpx;
  --space-sm: 16rpx;
  --space-md: 24rpx;
  --space-lg: 32rpx;
  --space-xl: 40rpx;
  
  /* 圆角 */
  --radius-sm: 12rpx;
  --radius-md: 16rpx;
  --radius-lg: 24rpx;
  --radius-round: 50%;
  
  /* 阴影 */
  --shadow-card: 0 4rpx 20rpx 0 rgba(0, 0, 0, 0.08);
  --shadow-modal: 0 12rpx 40rpx 0 rgba(0, 0, 0, 0.16);
}
```

### 20.2 工具类 (Utility Classes)

```css
/* 文本颜色 */
.text-primary { color: var(--text-primary); }
.text-secondary { color: var(--text-secondary); }
.text-success { color: var(--color-success); }
.text-error { color: var(--color-error); }

/* 背景颜色 */
.bg-page { background: var(--bg-page); }
.bg-white { background: var(--bg-container); }

/* 间距 */
.mt-sm { margin-top: var(--space-sm); }
.mt-md { margin-top: var(--space-md); }
.pt-lg { padding-top: var(--space-lg); }

/* 圆角 */
.radius-md { border-radius: var(--radius-md); }
.radius-lg { border-radius: var(--radius-lg); }

/* 阴影 */
.shadow-card { box-shadow: var(--shadow-card); }
```

---

## 附录

### A. 设计检查清单

**颜色**
- [ ] 主色调符合品牌定位
- [ ] 辅助色使用恰当
- [ ] 文字颜色对比度达标 (≥ 4.5:1)
- [ ] 数据色区分明显

**字体**
- [ ] 字号层级清晰 (3-5 级)
- [ ] 行高适中 (1.4-1.6)
- [ ] 字重使用合理

**间距**
- [ ] 基于 8rpx 栅格系统
- [ ] 页面边距统一 (30rpx)
- [ ] 组件间距一致

**圆角**
- [ ] 圆角大小适中
- [ ] 同类组件圆角统一

**阴影**
- [ ] 层次分明 (3-4 级)
- [ ] 颜色统一 (黑色半透明)

**交互**
- [ ] 点击区域 ≥ 88rpx
- [ ] 状态反馈明显
- [ ] 动画流畅自然

### B. 常见问题

**Q1: 为什么使用 rpx 而不是 px？**
A: rpx 是 uni-app 的响应式单位，会根据屏幕宽度自动换算，保证不同设备上的一致性。

**Q2: 如何处理 1px 边框问题？**
A: 使用 `border: 1rpx solid` 或通过伪元素 + `transform: scaleY(0.5)` 实现真 1px。

**Q3: 暗黑模式是否必须支持？**
A: 非必须，但建议在 V2.1 版本中考虑加入。

### C. 参考资源

- **Apple Human Interface Guidelines**: [https://developer.apple.com/design/](https://developer.apple.com/design/)
- **Material Design**: [https://material.io/design](https://material.io/design)
- **Ant Design**: [https://ant.design/](https://ant.design/)

---

**文档结束**

此设计规范为 uni-app x 开发提供完整的视觉指导，确保产品的视觉一致性和用户体验质量。
